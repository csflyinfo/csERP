# ERP 系统启动脚本使用说明

本目录下提供 3 个批处理脚本，双击运行即可，无需记忆命令。

## 📂 脚本清单

| 脚本 | 功能 | 使用场景 |
|---|---|---|
| **start.bat** | 一键启动系统 | 每天开始工作时 |
| **stop.bat** | 停止所有服务 | 结束工作/服务异常时 |
| **reset-db.bat** | 清空数据库 | 需要重新开始测试时 |

---

## 🚀 start.bat — 一键启动

**双击运行**，脚本会自动：

1. 释放 8080 / 5173 端口
2. 新窗口启动**后端**（Spring Boot）
3. 等待后端就绪（最多 30 秒）
4. 新窗口启动**前端**（Vite）
5. 自动**打开浏览器**访问 http://localhost:5173

启动完成后会显示登录信息：
- 账号：`admin`
- 密码：`admin123`

---

## 🛑 stop.bat — 停止服务

**双击运行**，脚本会：
- 停止 8080 端口的后端进程
- 停止 5173 端口的前端进程
- 关闭对应的命令窗口

---

## 🔄 reset-db.bat — 重置数据库

**双击运行**，会弹出确认提示 (Y/N)：
- 停止后端
- 删除数据库文件
- 下次启动会自动重建数据库，仅保留 admin 账号

⚠️ **警告**：此操作会清空所有商品、客户、订单等业务数据，不可恢复！

---

## ❓ 常见问题

### Q1: 双击 start.bat 后弹出 "mvn 不是内部或外部命令"
需要先安装 Maven 并配置到系统 PATH 环境变量。
或者已安装但当前用户 PATH 没生效，重启电脑试试。

### Q2: 双击 start.bat 后弹出 "npm 不是内部或外部命令"
需要先安装 Node.js（会自动带 npm）。
下载地址：https://nodejs.org/

### Q3: 后端启动很慢（超过 1 分钟）
首次启动 Maven 会下载依赖，可能需要 3-5 分钟。
之后启动会很快（约 15 秒）。

### Q4: 端口被占用无法启动
运行 `stop.bat` 或手动重启电脑释放端口。

### Q5: 浏览器打开显示白屏或 502
等待 5-10 秒后 Ctrl + F5 刷新（前端启动需要时间）。

---

## 💡 进阶：命令行手动启动

如果不想用批处理，也可以手动在 PowerShell 中执行：

```powershell
# 启动后端
cd E:\我的工作项目\erp-wms-tms\backend
mvn spring-boot:run

# 启动前端（新窗口）
cd E:\我的工作项目\erp-wms-tms\frontend
npm run dev
```
