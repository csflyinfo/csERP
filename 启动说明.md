# ERP 系统启动说明

这个仓库已经可以直接用仓库自带的 Maven Wrapper 启动后端，也可以使用批处理脚本一键启动前后端。

## 1. 环境要求

在本机先准备好以下环境：

- JDK 21
- Node.js 18+（自带 npm）
- Windows PowerShell

> 当前后端项目配置的 Java 版本是 21，因此请优先使用 JDK 21。

## 2. 推荐启动方式

### 方式 A：双击批处理脚本

仓库根目录下提供了 3 个脚本：

| 脚本 | 作用 |
|---|---|
| start.bat | 一键启动后端和前端 |
| stop.bat | 停止所有已启动服务 |
| reset-db.bat | 重置数据库 |

双击运行即可，脚本会自动：

1. 启动后端 Spring Boot 服务
2. 启动前端 Vite 服务
3. 打开浏览器访问 http://localhost:5173

启动成功后可用以下账号登录：

- 账号：admin
- 密码：admin123

### 方式 B：手动在 PowerShell 中启动

#### 启动后端

```powershell
cd E:\我的工作项目\erp-wms-tms
.\mvnw.cmd -f backend/pom.xml spring-boot:run
```

#### 启动前端

```powershell
cd E:\我的工作项目\erp-wms-tms\frontend
npm install
npm run dev
```

## 3. 验证命令

如果你想先确认环境和代码状态，可以运行：

```powershell
cd E:\我的工作项目\erp-wms-tms
.\mvnw.cmd -f backend/pom.xml test
```

当前已验证通过，测试结果为：

- 7 个测试全部通过
- 0 失败
- 0 错误

## 4. 常见问题

### Q1：提示 "mvn 不是内部或外部命令"
不用担心，这个项目已经支持 Maven Wrapper。请直接使用：

```powershell
.\mvnw.cmd -f backend/pom.xml ...
```

### Q2：提示 Java 版本不对
请安装并切换到 JDK 21。

### Q3：前端启动后页面空白
请等待 5~10 秒后刷新页面，首次启动时 Vite 需要初始化依赖。

### Q4：端口被占用
可以先运行 stop.bat，或者手动释放 8080 / 5173 端口。
