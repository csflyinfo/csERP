# 优化记录 — 验证环境统一到 5173：总账主干回灌采购发票分支，Flyway V93/V94 撞号顺延 V100/V101

- **日期**：2026-09-08
- **分支**：`feat/purchase-invoice`（PRD-30 采购发票，本地未 push）
- **背景**：总账（PRD-32）已 squash 合入 `main` 并 push（提交 `6d7075f`，迁移 V93~V99）。但日常验证环境 5173/8080 的主树 `E:\work\erp-wms-tms` 检出的是 `feat/purchase-invoice`（PRD-30），该分支从总账合入前分出，不含总账，故 5173 看不到「总账管理」。本次把 `main` 回灌进采购发票分支，使 **5173 一个环境同时具备 PRD-30 采购发票 + PRD-31 操作日志 + PRD-32 总账**，并停用 5174/8082 临时总账验证树。

## 1. 回灌合并

`git merge main`（主干→特性分支，保留双方历史，非 squash）。仅 1 处内容冲突：PRD 路线图 `00-PRD文档索引与版本路线图.md`（双方各追加 PRD 行），解决为 **PRD-30/31/32 三行全保留、按编号排列**。全部 Java/Vue/SQL 代码自动合并（FinanceController、PurchaseController、PurchaseReceiptController、SystemController、前端路由/菜单/API 等）。合并提交 `cfc335b`。

## 2. Flyway 撞号顺延（回灌后暴露）

并行三方都绕开了 V92（留给 PRD-31 操作日志），但采购发票后续增强与总账都取了 V93/V94：

| 版本 | 总账（main，已 push） | 采购发票（本地） | 处理 |
|---|---|---|---|
| V93 | `V93__fin_gl_account_init.sql` | `V93__pur_invoice_match_tax_rate_and_applied.sql` | 采购发票侧**顺延 V100** |
| V94 | `V94__fin_gl_voucher.sql` | `V94__pur_receipt_detail_invoiced.sql` | 采购发票侧**顺延 V101** |

总账 V93~V99 已合入并 push `main`（发布历史，不改），故由**未 push 的采购发票侧让位**：

- `git mv V93__pur_invoice_match_tax_rate_and_applied.sql → V100__…`、`V94__pur_receipt_detail_invoiced.sql → V101__…`；文件头版本号注释同步改。
- 这两个迁移只 ALTER 采购发票自身的表（`pur_invoice_match(_line)`、`pur_receipt_detail`），与总账 `fin_gl_*` 表无交集；顺延到 V100/V101 仍在其建表迁移 V90/V91 之后执行，顺序安全。
- 文档引用同步更正：`docs/采购发票管理-设计方案.md`、`docs/优化记录-PRD30-采购发票管理.md` 中的迁移文件名改为 V100/V101；PRD 路线图 PRD-30 行 Flyway 列补记 V100/V101 及顺延原因。

回灌后迁移连续无重复：**V89 入库统一、V90/V91 采购发票、V92 操作日志、V93~V99 总账、V100/V101 采购发票增强**，共 101 个版本。

## 3. 开发库空库重迁

主树 8080 用的开发库 `backend/data/erp-v1` 原按采购发票旧编号迁移（flyway 里记录的是 pur 的 V93/V94），与重编号后的类路径（V93/V94 已是总账）校验和不一致，Flyway 启动会报版本冲突。旧库已先备份为 `data/erp-v1.pre-gl-backfill-20260908-231557.mv.db`，再空库重迁 V1~V101。

## 4. 环境

- 统一后：主树 `E:\work\erp-wms-tms`（`feat/purchase-invoice`，含全部三 PRD）跑 **8080 后端 + 5173 前端（默认标题/徽标）**；
- 停用：总账临时验证树 5174/8082 与 `E:\work\erp-wms-tms-gl` 工作树（其 `frontend/node_modules` 是指向主树的 junction，撤树前先解链，勿删主树依赖）；
- 待采购发票分支以后正式 squash 合 `main` 时，V89/V90/V91/V100/V101 随之进主干（V100/V101 与总账 V93~V99 不撞号）。
