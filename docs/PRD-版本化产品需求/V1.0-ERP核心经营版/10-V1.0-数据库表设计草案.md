# V1.0 ERP核心经营版 — 数据库表设计草案

> 文档版本：v1.0  
> 编写日期：2026-06-13  
> 适用版本：V1.0 ERP核心经营版  
> 文档定位：面向后端开发、数据库设计、接口设计、测试造数的业务数据模型草案  
> 说明：本文为产品级数据库草案，不替代最终数据库DDL；最终字段类型、索引、分区、审计字段由技术设计阶段确认。

---

## 一、全局设计约定

### 1.1 主键与编码

| 项 | 约定 |
|---|---|
| 主键 | 统一使用 `varchar(32)`，建议雪花ID/UUID去横线 |
| 业务单号 | 独立字段，如 `order_no`、`bill_no`，不可作为主键 |
| 编码 | 商品、客户、供应商、仓库等使用独立业务编码 |
| 外键 | 产品文档中标注逻辑外键，数据库是否强外键由技术架构决定 |

### 1.2 金额、数量、时间

| 类型 | 建议类型 | 说明 |
|---|---|---|
| 金额 | decimal(18,2) | 订单金额、应收应付、收付款 |
| 单价/成本 | decimal(18,6) | 成本单价、换算后单价 |
| 数量 | decimal(18,4) | 支持称重和小数数量 |
| 比率 | decimal(8,4) | 税率、折扣率、毛利率 |
| 日期 | date | 单据日期、业务日期 |
| 时间 | datetime / timestamp | 创建、审核、操作时间 |

### 1.3 通用审计字段

所有业务表建议统一包含：

| 字段 | 说明 |
|---|---|
| created_by | 创建人ID |
| created_name | 创建人名称，冗余 |
| created_at | 创建时间 |
| updated_by | 修改人ID |
| updated_name | 修改人名称，冗余 |
| updated_at | 修改时间 |
| deleted | 逻辑删除标记，0否1是 |
| tenant_id | 租户ID，SaaS化预留 |
| owner_id | 货主ID，业务需要时使用 |
| version | 乐观锁版本号，关键单据建议使用 |

### 1.4 状态字段约定

状态字段统一使用字符串枚举，避免纯数字难读。

示例：

```text
DRAFT / PENDING_AUDIT / AUDITED / PARTIAL_IN / COMPLETED / CLOSED / CANCELLED
```

### 1.5 金额冗余原则

业务单据主表允许冗余汇总金额，明细表保留行金额。审核时应校验：

```text
主表金额 = 明细金额汇总
```

### 1.6 单据明细单位、仓库、成本字段统一要求

所有含商品明细且可能影响库存、毛利、成本追溯的业务单据明细表必须包含：

| 字段 | 建议类型 | 说明 |
|---|---|---|
| warehouse_id | varchar(32) | 明细仓库ID，调拨单除外 |
| warehouse_name | varchar(100) | 明细仓库名称，调拨单除外 |
| business_unit_id | varchar(32) | 业务开单单位ID |
| business_unit_name | varchar(50) | 业务开单单位名称，如箱、瓶、包 |
| business_qty | decimal(18,4) | 开单数量，用户录入 |
| conversion_rate | decimal(18,6) | 业务单位到基本单位换算率 |
| base_unit_id | varchar(32) | 商品基本单位ID |
| base_unit_name | varchar(50) | 商品基本单位名称 |
| base_qty | decimal(18,4) | 基本单位数量 = 开单数量 × 换算率 |
| cost_price | decimal(18,6) | 基本单位成本单价 |
| cost_amount | decimal(18,2) | 成本金额 = 基本单位数量 × 基本单位成本单价 |

适用单据包括：采购订单、采购入库、采购收货、采购退货、销售订单、销售出库、销售收货、销售退货、其他入库、其他出库、报损单、库存调整单、成本调整单。调拨单由单据头维护调出/调入仓，明细记录业务单位、开单数量、换算率、基本单位数量。

### 1.7 显示精度配置

系统存储精度不随展示设置降低；用户可调整数量、单价、金额显示位数，但只可增大，不可改小。建议增加精度配置表或使用系统参数表维护。

---

## 二、系统管理表

### 2.1 用户表 sys_user

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| user_id | varchar(32) | 是 | 主键 |
| account | varchar(50) | 是 | 登录账号，唯一 |
| user_name | varchar(100) | 是 | 用户名称 |
| password | varchar(255) | 是 | 加密密码 |
| employee_id | varchar(32) | 否 | 绑定人员 |
| mobile | varchar(20) | 是 | 手机号 |
| email | varchar(100) | 否 | 邮箱 |
| group_id | varchar(32) | 是 | 权限组 |
| is_custom_permission | tinyint | 是 | 是否自定义权限 |
| custom_permission_json | text | 否 | 自定义权限JSON |
| status | varchar(20) | 是 | NORMAL/STOPPED/LOCKED |
| lock_time | datetime | 否 | 锁定时间 |
| last_login_time | datetime | 否 | 最后登录时间 |
| last_login_ip | varchar(50) | 否 | 最后登录IP |
| pwd_update_time | datetime | 否 | 密码更新时间 |
| created_by/created_at | 通用 | 是 | 审计字段 |

建议索引：`account`唯一索引、`mobile`索引、`group_id`索引。

### 2.2 用户数据范围表 sys_user_data_scope

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| id | varchar(32) | 是 | 主键 |
| user_id | varchar(32) | 是 | 用户ID |
| scope_type | varchar(50) | 是 | WAREHOUSE/CUSTOMER/SALESMAN/SUPPLIER/GOODS_CATEGORY/BRAND/OWNER |
| scope_value | text | 是 | 范围值，多个ID逗号分隔，ALL表示全部 |
| scope_name | varchar(500) | 否 | 范围名称冗余 |

### 2.3 权限组表 sys_permission_group

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| group_id | varchar(32) | 是 | 主键 |
| group_code | varchar(50) | 是 | 权限组编码，唯一 |
| group_name | varchar(100) | 是 | 权限组名称 |
| is_system | tinyint | 是 | 是否系统内置 |
| status | varchar(20) | 是 | NORMAL/STOPPED |
| remark | varchar(500) | 否 | 备注 |

### 2.4 模块权限表 sys_module_permission

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| id | varchar(32) | 是 | 主键 |
| group_id | varchar(32) | 是 | 权限组ID |
| module_code | varchar(100) | 是 | 模块编码 |
| module_name | varchar(100) | 是 | 模块名称 |
| is_granted | tinyint | 是 | 是否授权 |
| sort_order | int | 否 | 排序 |

### 2.5 功能权限表 sys_function_permission

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| id | varchar(32) | 是 | 主键 |
| group_id | varchar(32) | 是 | 权限组ID |
| module_code | varchar(100) | 是 | 模块编码 |
| func_code | varchar(50) | 是 | VIEW/ADD/EDIT/DELETE/AUDIT/UNAUDIT/PRINT/IMPORT/EXPORT |
| func_name | varchar(100) | 是 | 功能名称 |
| is_granted | tinyint | 是 | 是否授权 |

### 2.6 字段权限表 sys_field_permission

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| id | varchar(32) | 是 | 主键 |
| group_id | varchar(32) | 是 | 权限组ID |
| field_code | varchar(100) | 是 | VIEW_COST/VIEW_PROFIT/VIEW_PURCHASE_PRICE等 |
| field_name | varchar(100) | 是 | 字段权限名称 |
| is_granted | tinyint | 是 | 是否授权 |

### 2.7 系统参数表 sys_config

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| config_id | varchar(32) | 是 | 主键 |
| config_key | varchar(100) | 是 | 参数键，唯一 |
| config_name | varchar(100) | 是 | 参数名称 |
| config_value | text | 是 | 当前值 |
| default_value | text | 是 | 默认值 |
| config_type | varchar(20) | 是 | STRING/INT/DECIMAL/BOOLEAN/SELECT/TIME |
| option_values | text | 否 | 可选值JSON |
| config_group | varchar(50) | 是 | 参数分组 |
| remark | varchar(500) | 否 | 说明 |
| is_system | tinyint | 是 | 系统内置 |

### 2.8 操作日志表 sys_operation_log

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| log_id | varchar(32) | 是 | 主键 |
| user_id | varchar(32) | 否 | 用户ID |
| user_account | varchar(50) | 是 | 操作账号 |
| user_name | varchar(100) | 否 | 操作人 |
| module_code | varchar(100) | 否 | 模块编码 |
| module_name | varchar(100) | 是 | 模块名称 |
| operation_type | varchar(50) | 是 | LOGIN/ADD/EDIT/AUDIT/EXPORT等 |
| operation_content | varchar(2000) | 是 | 操作摘要 |
| bill_type | varchar(100) | 否 | 单据类型 |
| bill_id | varchar(32) | 否 | 单据ID |
| bill_no | varchar(100) | 否 | 单据号 |
| request_ip | varchar(50) | 否 | IP |
| request_url | varchar(500) | 否 | 请求地址 |
| request_params | text | 否 | 脱敏参数 |
| operation_result | varchar(20) | 是 | SUCCESS/FAIL |
| fail_reason | varchar(500) | 否 | 失败原因 |
| cost_time_ms | int | 否 | 耗时 |
| created_at | datetime | 是 | 操作时间 |

### 2.9 字典类型表 sys_dictionary_type

**V7 新建**。字典模块左侧网格数据源。

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| id | varchar(32) | 是 | 主键 |
| dict_type | varchar(50) | 是 | 字典类型编码，唯一（如 `delivery_method`） |
| dict_type_name | varchar(100) | 是 | 字典类型名称（如「交货方式」） |
| description | varchar(300) | 否 | 说明 |
| is_system | tinyint(1) | 否 | 是否系统预置；预置类型仅能改名称，不能删/停 |
| status | varchar(20) | 是 | NORMAL/STOPPED |
| created_at | timestamp | 否 | 创建时间 |

V1.0 系统预置类型：`delivery_method`（交货方式）/ `logistics_company`（物流公司）/ `customer_channel`（客户渠道）/ `supplier_type`（供应商类型）/ `settlement_method`（结算方式）等。

### 2.10 字典值表 sys_dictionary

**V6 新建，V7 与 V10 补 seed**。字典模块右侧网格数据源。

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| id | varchar(32) | 是 | 主键 |
| dict_type | varchar(50) | 是 | 所属字典类型 |
| dict_code | varchar(50) | 是 | 字典编码；系统预置值按顺序号从 0 递增（V8 起） |
| dict_name | varchar(100) | 是 | 字典名称 |
| sort_order | int | 否 | 排序 |
| is_system | tinyint(1) | 否 | 是否系统预置值；预置值仅能改名称/顺序，不能删（可停用） |
| remark | varchar(200) | 否 | 备注 |
| status | varchar(20) | 是 | NORMAL/STOPPED；公共取值 `getDict()` 仅返回 NORMAL |
| created_at | timestamp | 否 | 创建时间 |

唯一约束：`(dict_type, dict_code)`；索引 `(dict_type)`。

**公共取值 API**：`GET /base/dictionary?type=xxx` 返回 `[{ code, name, sortOrder }]`，只含 status=NORMAL。前端封装 `utils/dictionary.js` 提供 `getDict(type)` 带 5 分钟内存缓存；字典维护后调 `invalidateDict(type)` 主动失效。

---

## 三、基础资料表

### 3.1 部门表 base_department

|字段|类型|必填|说明|
|---|---|:---:|---|
|department_id|varchar(32)|是|主键|
|department_code|varchar(50)|是|部门编码，唯一|
|department_name|varchar(100)|是|部门名称|
|parent_code|varchar(50)|否|上级部门编码（顶级部门为空/NULL），实现树形结构|
|head_count|int|否|人数（列表参考展示）|
|remark|varchar(500)|否|备注|
|status|varchar(20)|是|NORMAL/STOPPED|
|created_at|timestamp|否|创建时间|

> V1.0 采用「左树右表」交互（同商品分类）。新增/编辑时上级部门可选空表示顶级。列表「上级部门」列显示名称。

### 3.2 人员表 base_employee

|字段|类型|必填|说明|
|---|---|:---:|---|
|employee_id|varchar(32)|是|主键|
|employee_code|varchar(50)|是|人员编码，唯一。留空由后台按 `E00001` 递增流水生成|
|employee_name|varchar(100)|是|姓名|
|gender|varchar(10)|否|性别（男/女）|
|owner_name|varchar(100)|否|所属货主|
|mobile|varchar(50)|否|手机号|
|id_card|varchar(50)|否|身份证号|
|education|varchar(50)|否|学历|
|address|varchar(200)|否|联系地址|
|department|varchar(100)|否|所属部门（存部门名称）|
|position|varchar(100)|否|职位|
|is_salesman|tinyint(1)|是|是否业务员|
|is_salesman_admin|tinyint(1)|是|是否业务员管理员（仅 is_salesman=1 时启用）|
|parent_salesman|varchar(100)|否|上级业务员（仅 is_salesman=1 时启用；存人员姓名）|
|is_buyer|tinyint(1)|是|是否采购员|
|is_warehouse_keeper|tinyint(1)|是|是否库管员|
|is_deliveryman|tinyint(1)|是|是否配送员|
|remark|varchar(500)|否|备注|
|status|varchar(20)|是|NORMAL/STOPPED|

### 3.3 货主表 base_owner

|字段|类型|必填|说明|
|---|---|:---:|---|
|owner_id|varchar(32)|是|主键|
|owner_code|varchar(50)|是|货主编码，唯一。留空由后台按 `H00001` 递增流水生成|
|owner_name|varchar(100)|是|货主名称|
|owner_type|varchar(50)|否|货主类型（自营/联营/代销）|
|platform|varchar(100)|否|所属平台|
|remark|varchar(500)|否|备注|
|status|varchar(20)|是|NORMAL/STOPPED|

> 系统首次启动自动创建默认货主 `H0001 平台`。

### 3.4 单位表 base_unit

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| unit_id | varchar(32) | 是 | 主键 |
| unit_code | varchar(50) | 是 | 单位编码 |
| unit_name | varchar(50) | 是 | 单位名称 |
| can_base_unit | tinyint(1) | 是 | 是否可作基本单位（V1.0 恒为 1，前端不再暴露该字段） |
| can_middle_unit | tinyint(1) | 是 | 是否中单位：1=允许被商品选为中单位 |
| can_large_unit | tinyint(1) | 是 | 是否大单位：1=允许被商品选为大单位 |
| goods_count | int | 否 | 引用该单位的商品数（列表参考） |
| status | varchar(20) | 是 | NORMAL/STOPPED |

> 兼容说明：老版本表中的 `is_large_unit / is_mid_unit` 字段已并入 `can_large_unit / can_middle_unit`；`can_base_unit` 保留仅为兼容旧数据，V1.0 新建/编辑单位时后台强制置 1。

### 3.5 商品分类表 base_goods_category

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| category_id | varchar(32) | 是 | 主键 |
| parent_id | varchar(32) | 否 | 上级分类 |
| category_code | varchar(50) | 是 | 分类编码 |
| category_name | varchar(100) | 是 | 分类名称 |
| level | int | 是 | 层级 |
| is_leaf | tinyint | 是 | 是否末级 |
| default_tax_rate | decimal(8,4) | 否 | 默认税率 |
| status | varchar(20) | 是 | NORMAL/STOPPED |

### 3.6 品牌表 base_brand

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| brand_id | varchar(32) | 是 | 主键 |
| brand_code | varchar(50) | 是 | 品牌编码 |
| brand_name | varchar(100) | 是 | 品牌名称 |
| short_spell | varchar(50) | 否 | 简拼 |
| logo_url | varchar(500) | 否 | Logo |
| status | varchar(20) | 是 | NORMAL/STOPPED |

### 3.7 商品主表 base_goods

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| goods_id | varchar(32) | 是 | 主键 |
| goods_code | varchar(50) | 是 | 商品编码，唯一 |
| goods_name | varchar(200) | 是 | 商品名称 |
| spec | varchar(200) | 否 | 规格型号 |
| short_spell | varchar(50) | 否 | 简拼 |
| category_id | varchar(32) | 是 | 商品分类，末级 |
| brand_id | varchar(32) | 否 | 品牌 |
| base_unit_id | varchar(32) | 是 | 基本单位 |
| base_barcode | varchar(100) | 否 | 基本单位条码 |
| shelf_life_days | int | 否 | 保质期天数 |
| goods_type | varchar(50) | 是 | NORMAL/GIFT/CONSUMABLE/PACKAGE |
| storage_attr | varchar(50) | 否 | NORMAL_TEMP/COLD/FROZEN |
| is_returnable | tinyint | 是 | 是否可退 |
| is_weighable | tinyint | 是 | 是否称重 |
| is_salable | tinyint | 是 | 是否可售 |
| is_purchasable | tinyint | 是 | 是否可采购 |
| standard_price | decimal(18,2) | 是 | 标准售价 |
| reference_purchase_price | decimal(18,2) | 否 | 参考进价 |
| min_sale_price | decimal(18,2) | 否 | 最低售价 |
| suggested_retail_price | decimal(18,2) | 否 | 建议零售价 |
| min_order_qty | decimal(18,4) | 否 | 起订量 |
| order_multiple | decimal(18,4) | 否 | 起订倍数 |
| stock_upper_limit | decimal(18,4) | 否 | 库存上限 |
| stock_lower_limit | decimal(18,4) | 否 | 库存下限 |
| expiry_warning_days | int | 否 | 临期预警天数 |
| default_supplier_id | varchar(32) | 否 | 默认供应商 |
| default_warehouse_id | varchar(32) | 否 | 默认仓库 |
| owner_id | varchar(32) | 是 | 货主 |
| main_image_url | varchar(500) | 否 | 主图 |
| status | varchar(20) | 是 | NORMAL/STOPPED |

### 3.8 商品多单位表 base_goods_unit

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| goods_unit_id | varchar(32) | 是 | 主键 |
| goods_id | varchar(32) | 是 | 商品ID |
| unit_id | varchar(32) | 是 | 单位ID |
| unit_type | varchar(20) | 是 | BASE/MID/LARGE |
| barcode | varchar(100) | 否 | 单位条码，唯一 |
| conversion_rate | decimal(18,6) | 是 | 换算率，相对基本单位 |
| weight | decimal(18,6) | 否 | 重量 |
| volume | decimal(18,6) | 否 | 体积 |
| price1-price10 | decimal(18,2) | 否 | 价格组价格，逻辑字段 |
| is_default_purchase | tinyint | 是 | 默认采购单位 |
| is_default_sale | tinyint | 是 | 默认销售单位 |

### 3.9 客户表 base_customer

V1.0 实际字段（按代码实现整理，与旧版本设计有差异；老列已注释在注释里）：

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| customer_id | varchar(32) | 是 | 主键 |
| customer_code | varchar(50) | 是 | 客户编码，唯一 |
| customer_name | varchar(100) | 是 | 客户名称 |
| channel_type | varchar(50) | 否 | 渠道类型（字典 customer_channel，存值） |
| contact_name | varchar(100) | 否 | 联系人 |
| mobile | varchar(30) | 否 | 手机号 |
| territory | varchar(100) | 否 | 片区（存名称；下拉限末级片区） |
| route_line | varchar(100) | 否 | 线路（存名称） |
| salesman | varchar(100) | 否 | 业务员（存姓名；下拉限 `is_salesman=true`） |
| customer_level | varchar(50) | 否 | 客户等级 |
| shipping_address | varchar(500) | 否 | **V10** 收货地址；抽屉里独占一行 |
| longitude | decimal(11,7) | 否 | **V11** 经度（收货地址坐标） |
| latitude | decimal(10,7) | 否 | **V11** 纬度 |
| price_group_code | varchar(50) | 否 | **V4** 价格组编码；新建默认空=商品默认售价；下拉仅列 `enabled=true` |
| credit_limit | decimal(18,2) | 否 | 信用额度 |
| ar_balance | decimal(18,2) | 否 | 应收余额 |
| overdue_amount | decimal(18,2) | 否 | 逾期金额 |
| invoice_title | varchar(200) | 否 | 发票抬头 |
| tax_no | varchar(100) | 否 | 税号 |
| settlement_type | varchar(20) | 是 | **V9** PREPAY / COD / TERM，见账期规范 |
| term_type | varchar(20) | 否 | **V9** FIXED / WEEKLY / SEMI_MONTH / MONTHLY（settlement_type=TERM 时必填） |
| term_days | int | 否 | **V9** 账期天数 |
| cutoff_day | int | 否 | **V9** 截账日（1-31，MONTHLY 时必填） |
| payment_mode | varchar(4) | 否 | **V9** A / B（MONTHLY 时必填） |
| term_months | int | 否 | **V9** 账期月数（MONTHLY 模式B用） |
| payment_day | int | 否 | **V9** 付款日（1-31，MONTHLY 模式B用） |
| status | varchar(20) | 是 | NORMAL/STOPPED；编辑时可切换 |

结算方式列表展示派生列 `settlementText`，规则见 [账期管理-产品说明](../../账期管理-产品说明.md)。列表列包含"价格组"（后端派生 `priceGroupName`）。

### 3.10 客户地址表 base_customer_address

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| address_id | varchar(32) | 是 | 主键 |
| customer_id | varchar(32) | 是 | 客户ID |
| address_name | varchar(100) | 是 | 地址别名 |
| contact_person | varchar(50) | 是 | 收货联系人 |
| contact_mobile | varchar(20) | 是 | 电话 |
| province/city/district | varchar(50) | 否 | 省市区 |
| detail_address | varchar(300) | 是 | 详细地址 |
| lng | decimal(10,6) | 否 | 经度 |
| lat | decimal(10,6) | 否 | 纬度 |
| is_default | tinyint | 是 | 是否默认 |
| status | varchar(20) | 是 | NORMAL/STOPPED |

### 3.11 供应商表 base_supplier

V1.0 实际字段（按代码实现整理）：

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| supplier_id | varchar(32) | 是 | 主键 |
| supplier_code | varchar(50) | 是 | 供应商编码，唯一 |
| supplier_name | varchar(100) | 是 | 供应商名称 |
| short_name | varchar(100) | 否 | 简称 |
| supplier_type | varchar(50) | 否 | 供应商类型（字典 supplier_type） |
| contact_name | varchar(100) | 否 | 联系人 |
| phone | varchar(30) | 否 | 电话 |
| delivery_days | int | 否 | 到货天数 |
| default_buyer | varchar(100) | 否 | 默认采购员姓名（下拉限 `is_buyer=true`） |
| delivery_method | varchar(50) | 否 | **V6** 交货方式（字典 delivery_method，默认「送货上门」） |
| default_logistics_company | varchar(100) | 否 | **V6** 默认物流公司（字典 logistics_company） |
| address | varchar(500) | 否 | 地址 |
| remark | varchar(500) | 否 | 备注 |
| ap_balance | decimal(18,2) | 否 | 应付余额 |
| settlement_type | varchar(20) | 是 | **V9** PREPAY / COD / TERM |
| term_type | varchar(20) | 否 | **V9** FIXED / WEEKLY / SEMI_MONTH / MONTHLY |
| term_days | int | 否 | **V9** 账期天数 |
| cutoff_day | int | 否 | **V9** 截账日 |
| payment_mode | varchar(4) | 否 | **V9** A / B |
| term_months | int | 否 | **V9** 账期月数 |
| payment_day | int | 否 | **V9** 付款日 |
| status | varchar(20) | 是 | NORMAL/STOPPED |

> 说明：V6 起前端表单不再维护 `invoice_title / tax_no`（税务信息保留列，仅前端隐藏，为兼容旧数据）；供应商收款账户拆到 3.11.1 子表管理。

### 3.11.1 供应商收款账户表 base_supplier_bank_account

**V6 新建**。同一供应商可维护多条收款账户。

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| id | varchar(32) | 是 | 主键 |
| supplier_code | varchar(50) | 是 | 供应商编码 |
| account_name | varchar(100) | 是 | 户名 |
| bank_name | varchar(100) | 否 | 开户银行 |
| bank_account | varchar(100) | 是 | 银行账号 |
| branch | varchar(100) | 否 | 支行 |
| is_default | tinyint(1) | 否 | 是否默认账户 |
| remark | varchar(200) | 否 | 备注 |
| sort_order | int | 否 | 排序 |
| created_at | timestamp | 否 | 创建时间 |

保存策略：供应商抽屉里的银行账户网格是**全量替换** — 每次保存删除该供应商所有账户后重新插入，避免脏数据。前端支持行内新增/删除/编辑；同一供应商只允许一个 `is_default=1`。

### 3.12 仓库表 base_warehouse

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| warehouse_id | varchar(32) | 是 | 主键 |
| warehouse_code | varchar(50) | 是 | 仓库编码 |
| warehouse_name | varchar(100) | 是 | 仓库名称 |
| warehouse_type | varchar(50) | 是 | NORMAL/VIRTUAL |
| stock_type | varchar(50) | 是 | MAIN/RETURN/DAMAGE/EXPIRING |
| cost_group | varchar(50) | 是 | 成本分组 |
| owner_id | varchar(32) | 否 | 货主 |
| address | varchar(300) | 否 | 地址 |
| manager_id | varchar(32) | 否 | 负责人 |
| is_default | tinyint | 是 | 是否默认 |
| status | varchar(20) | 是 | NORMAL/STOPPED |

### 3.13 客户价格表 base_customer_price

客户价格由客户价格调整单审核生成，当前表保存最终可匹配的客户商品价格明细。

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| price_id | varchar(32) | 是 | 主键 |
| adjust_id | varchar(32) | 是 | 来源客户价格调整单ID |
| adjust_detail_id | varchar(32) | 是 | 来源调整单明细ID |
| customer_id | varchar(32) | 是 | 客户ID |
| goods_id | varchar(32) | 是 | 商品ID |
| unit_id | varchar(32) | 是 | 基本单位ID |
| original_price | decimal(18,2) | 是 | 原价，取客户价格组默认价，无则取标准价 |
| price | decimal(18,2) | 是 | 现价/客户专属价格 |
| latest_purchase_price | decimal(18,2) | 否 | 最新进价 |
| cost_price | decimal(18,2) | 否 | 成本价 |
| effective_mode | varchar(20) | 是 | IMMEDIATE/SCHEDULED |
| effective_time | datetime | 是 | 生效时间点 |
| valid_from | date | 否 | 有效开始日期 |
| valid_to | date | 否 | 有效结束日期 |
| status | varchar(20) | 是 | PENDING/EFFECTIVE/STOPPED/EXPIRED |
| stop_reason | varchar(500) | 否 | 停用原因 |
| stopped_by | varchar(32) | 否 | 停用人 |
| stopped_at | datetime | 否 | 停用时间 |

### 3.13.1 客户价格调整单表 base_customer_price_adjust

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| adjust_id | varchar(32) | 是 | 主键 |
| adjust_no | varchar(50) | 是 | 调整单号 |
| customer_id | varchar(32) | 是 | 客户ID |
| bill_date | date | 是 | 单据日期 |
| effective_mode | varchar(20) | 是 | IMMEDIATE/SCHEDULED |
| effective_time | datetime | 否 | 定时生效时间点 |
| valid_type | varchar(20) | 是 | LONG_TERM/RANGE |
| valid_from | date | 否 | 有效开始日期 |
| valid_to | date | 否 | 有效结束日期 |
| remark | varchar(500) | 否 | 备注 |
| status | varchar(20) | 是 | DRAFT/PENDING/APPROVED/CANCELLED |
| creator_id | varchar(32) | 是 | 制单人 |
| created_at | datetime | 是 | 制单时间 |
| auditor_id | varchar(32) | 否 | 审核人 |
| audit_time | datetime | 否 | 审核时间 |

### 3.13.2 客户价格调整单明细表 base_customer_price_adjust_detail

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| detail_id | varchar(32) | 是 | 主键 |
| adjust_id | varchar(32) | 是 | 调整单ID |
| goods_id | varchar(32) | 是 | 商品ID |
| unit_id | varchar(32) | 是 | 基本单位ID |
| original_price | decimal(18,2) | 是 | 原价 |
| current_price | decimal(18,2) | 是 | 现价 |
| latest_purchase_price | decimal(18,2) | 否 | 最新进价 |
| cost_price | decimal(18,2) | 否 | 成本价 |
| line_remark | varchar(500) | 否 | 行备注 |

### 3.14 资金账户表 base_fund_account

|字段|类型|必填|说明|
|---|---|:---:|---|
|fund_account_id|varchar(32)|是|主键|
|fund_account_code|varchar(50)|是|账户编码，唯一。留空由后台按 `A + 6 位十六进制` 生成|
|fund_account_name|varchar(100)|是|账户名称|
|parent_code|varchar(50)|否|上级账户编码；顶级（一级账户）为空|
|account_type|varchar(50)|否|现金 / 银行账户 / 在途 / 第三方支付|
|balance|decimal(18,2)|否|期初余额，默认 0|
|remark|varchar(500)|否|备注|
|is_system|tinyint(1)|否|是否系统内置一级账户；true 时不允许编辑/删除/停用|
|status|varchar(20)|是|NORMAL/STOPPED|
|created_at|timestamp|否|创建时间|

**V1.0 系统预置一级账户**（`is_system=true`）：

|编码|名称|
|---|---|
|01|现金|
|02|银行卡|
|03|微信|
|04|支付宝|
|05|三方账户|
|06|其他|

规则：

- 一级账户仅由系统预置，前端不提供在同级新增/删除入口。
- 新建二级账户时**上级账户必填**，后端强制校验，未填返回 400。
- 树状展示（同「部门管理」），左树右表。
- 停用一级账户不允许（列表操作列显示"系统默认"标记，无编辑/删除按钮）。

### 3.15 费用类型表 base_expense_type

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| expense_type_id | varchar(32) | 是 | 主键 |
| parent_id | varchar(32) | 否 | 上级费用类型 |
| expense_type_code | varchar(50) | 是 | 费用类型编码 |
| expense_type_name | varchar(100) | 是 | 费用类型名称 |
| direction | varchar(20) | 是 | INCOME/EXPENSE/BOTH |
| is_purchase_expense | tinyint | 是 | 是否采购费用 |
| affect_cost | tinyint | 是 | 是否参与成本 |
| status | varchar(20) | 是 | NORMAL/STOPPED |

### 3.16 往来单位表 base_counterparty

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| counterparty_id | varchar(32) | 是 | 主键 |
| counterparty_code | varchar(50) | 是 | 往来单位编码 |
| counterparty_name | varchar(200) | 是 | 往来单位名称 |
| counterparty_type | varchar(50) | 是 | LOGISTICS/PROPERTY/PERSON/SERVICE/OTHER |
| contact_person | varchar(50) | 否 | 联系人 |
| contact_mobile | varchar(20) | 否 | 联系电话 |
| address | varchar(300) | 否 | 地址 |
| settlement_type | varchar(50) | 否 | 结算方式 |
| settlement_days | int | 否 | 账期天数 |
| invoice_title | varchar(200) | 否 | 发票抬头 |
| tax_no | varchar(100) | 否 | 税号 |
| bank_name | varchar(100) | 否 | 开户行 |
| bank_account | varchar(100) | 否 | 银行账号 |
| status | varchar(20) | 是 | NORMAL/STOPPED |

### 3.17 片区表 base_territory

|字段|类型|必填|说明|
|---|---|:---:|---|
|territory_id|varchar(32)|是|主键|
|territory_code|varchar(50)|是|片区编码，唯一|
|territory_name|varchar(100)|是|片区名称|
|parent_code|varchar(50)|否|上级片区编码（顶级为空），实现树状层级（V3 迁移新增）|
|remark|varchar(500)|否|备注|
|status|varchar(20)|是|NORMAL/STOPPED|
|created_at|timestamp|否|创建时间|

> V1.0 采用「左树右表」交互（同商品分类）。原「所属城市」「覆盖范围」字段废弃（业务反馈冗余）。删除时后端校验有无下级。

### 3.18 线路表 base_route_line

|字段|类型|必填|说明|
|---|---|:---:|---|
|route_line_id|varchar(32)|是|主键|
|route_line_code|varchar(50)|是|线路编码，唯一|
|route_line_name|varchar(100)|是|线路名称|
|driver|varchar(100)|否|默认司机|
|coverage|varchar(200)|否|覆盖范围|
|remark|varchar(500)|否|备注|
|status|varchar(20)|是|NORMAL/STOPPED|
|created_at|timestamp|否|创建时间|

### 3.19 价格组表 base_price_group

|字段|类型|必填|说明|
|---|---|:---:|---|
|price_group_id|varchar(32)|是|主键|
|price_group_code|varchar(50)|是|价格组编码，唯一|
|price_group_name|varchar(100)|是|价格组名称|
|enabled|tinyint(1)|是|是否启用（通过独立启停端点切换）|
|sort_order|int|否|排序|
|remark|varchar(500)|否|备注|
|is_system|tinyint(1)|否|是否系统内置（V1.0 预置 10 条配送价 PG01~PG10，均为 true）|
|status|varchar(20)|是|NORMAL/STOPPED|
|created_at|timestamp|否|创建时间|

**V1.0 系统预置**（Flyway V4）：`PG01~PG10 配送价1~配送价10`，`is_system=true`，初始 `enabled=false`。不允许新建/删除；编辑仅可改 `price_group_name / sort_order / remark`；启停通过 `/api/base/master/price-group/enable`。停用时清空 `base_customer.price_group_code`。

### 3.20 价格组商品价格表 base_price_group_item

|字段|类型|必填|说明|
|---|---|:---:|---|
|id|varchar(32)|是|主键|
|goods_code|varchar(50)|是|商品编码|
|goods_name|varchar(200)|否|商品名称（冗余展示）|
|price_group_code|varchar(50)|是|价格组编码|
|unit_level|tinyint|是|1-小 / 2-中 / 3-大|
|price|decimal(14,4)|是|售价|
|is_active|tinyint(1)|是|是否启用；停用仅影响展示/开单引用，不删除|
|created_at|timestamp|否|创建时间|
|updated_at|timestamp|否|更新时间|

唯一约束：`(goods_code, price_group_code, unit_level)`。索引：`(price_group_code, is_active)`、`(goods_code)`。首次由「调价单审核通过」写入；后续通过「价格组商品查询」页启停或后续调价单更新。

### 3.21 调价单头表 base_price_adjust_order

|字段|类型|必填|说明|
|---|---|:---:|---|
|order_id|varchar(32)|是|主键|
|order_no|varchar(30)|是|调价单号，唯一，规则 `TJ + yyyyMMdd + 3位流水`|
|price_group_code|varchar(50)|是|目标价格组编码|
|price_group_name|varchar(100)|否|冗余名称|
|goods_count|int|否|明细行数|
|status|varchar(20)|是|`DRAFT` / `PENDING` / `APPROVED` / `REJECTED`|
|remark|varchar(500)|否|备注|
|reject_reason|varchar(500)|否|驳回原因（仅 REJECTED 状态）|
|create_user|varchar(50)|是|制单人|
|create_time|timestamp|是|制单时间|
|submit_user|varchar(50)|否|提交人|
|submit_time|timestamp|否|提交时间|
|audit_user|varchar(50)|否|审核人|
|audit_time|timestamp|否|审核时间|

### 3.22 调价单明细表 base_price_adjust_order_item

|字段|类型|必填|说明|
|---|---|:---:|---|
|id|varchar(32)|是|主键|
|order_id|varchar(32)|是|调价单ID|
|goods_code|varchar(50)|是|商品编码|
|goods_name|varchar(200)|否|商品名称|
|small_new_price|decimal(14,4)|否|小单位新价格，NULL 表示不调整|
|medium_new_price|decimal(14,4)|否|中单位新价格|
|large_new_price|decimal(14,4)|否|大单位新价格|
|small_old_price|decimal(14,4)|否|小单位原价快照（制单时快照）|
|medium_old_price|decimal(14,4)|否|中单位原价快照|
|large_old_price|decimal(14,4)|否|大单位原价快照|

每行至少一个 `*_new_price` 有值（前端强制校验）。

### 3.23 变价日志表 base_price_change_log

|字段|类型|必填|说明|
|---|---|:---:|---|
|id|varchar(32)|是|主键|
|order_id|varchar(32)|否|关联调价单ID|
|order_no|varchar(30)|否|冗余调价单号|
|goods_code|varchar(50)|是|商品编码|
|goods_name|varchar(200)|否|商品名称|
|price_group_code|varchar(50)|是|价格组编码|
|price_group_name|varchar(100)|否|冗余价格组名称|
|unit_level|tinyint|是|1-小 / 2-中 / 3-大|
|old_price|decimal(14,4)|否|变价前价格，首次设价为 NULL|
|new_price|decimal(14,4)|是|变价后价格|
|change_type|varchar(30)|是|`ADJUST_INIT` 首次设价 / `ADJUST_UPDATE` 调价单变更|
|operator|varchar(50)|是|操作人（审核人）|
|remark|varchar(500)|否|变更备注（继承调价单备注）|
|created_at|timestamp|否|操作时间|

索引：`(price_group_code)`、`(goods_code)`、`(created_at)`。变价查询页支持按价格组、商品、调价单号、操作人筛选，xlsx 导出上限 10000 条。

---

## 四、采购业务表

### 4.1 采购订单主表 purchase_order

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| order_id | varchar(32) | 是 | 主键 |
| order_no | varchar(50) | 是 | 采购单号 |
| supplier_id | varchar(32) | 是 | 供应商 |
| supplier_name | varchar(200) | 是 | 冗余名称 |
| purchaser_id | varchar(32) | 否 | 采购员 |
| warehouse_id | varchar(32) | 是 | 收货仓库 |
| owner_id | varchar(32) | 是 | 货主 |
| order_date | date | 是 | 单据日期 |
| expected_arrival_date | date | 否 | 预计到货 |
| goods_amount | decimal(18,2) | 是 | 商品金额 |
| tax_amount | decimal(18,2) | 是 | 税额 |
| discount_amount | decimal(18,2) | 是 | 折扣金额 |
| final_amount | decimal(18,2) | 是 | 最终金额 |
| settlement_type | varchar(50) | 是 | 结算方式 |
| status | varchar(50) | 是 | DRAFT/PENDING_AUDIT/AUDITED/PARTIAL_IN/COMPLETED/TERMINATED/CLOSED |
| inbound_status | varchar(50) | 是 | NOT_IN/PARTIAL_IN/ALL_IN |
| payable_status | varchar(50) | 是 | NOT_GENERATED/PARTIAL/GENERATED |
| remark | varchar(500) | 否 | 备注 |
| auditor_id | varchar(32) | 否 | 审核人 |
| audit_time | datetime | 否 | 审核时间 |

### 4.2 采购订单明细 purchase_order_detail

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| detail_id | varchar(32) | 是 | 主键 |
| order_id | varchar(32) | 是 | 采购订单ID |
| line_no | int | 是 | 行号 |
| goods_id | varchar(32) | 是 | 商品 |
| goods_code/name/spec | varchar | 是 | 冗余商品信息 |
| unit_id | varchar(32) | 是 | 采购单位 |
| unit_name | varchar(50) | 是 | 单位名 |
| conversion_rate | decimal(18,6) | 是 | 换算率 |
| quantity | decimal(18,4) | 是 | 采购数量 |
| base_quantity | decimal(18,4) | 是 | 基本单位数量 |
| price | decimal(18,6) | 是 | 含税单价 |
| tax_rate | decimal(8,4) | 否 | 税率 |
| amount | decimal(18,2) | 是 | 含税金额 |
| received_quantity | decimal(18,4) | 是 | 已入库数量 |
| returned_quantity | decimal(18,4) | 是 | 已退数量 |
| line_type | varchar(50) | 是 | NORMAL/GIFT/SAMPLE |
| remark | varchar(200) | 否 | 备注 |

### 4.3 采购入库主表 purchase_inbound

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| inbound_id | varchar(32) | 是 | 主键 |
| inbound_no | varchar(50) | 是 | 入库单号 |
| order_id | varchar(32) | 是 | 采购订单ID |
| order_no | varchar(50) | 是 | 采购单号 |
| supplier_id | varchar(32) | 是 | 供应商 |
| warehouse_id | varchar(32) | 是 | 入库仓库 |
| owner_id | varchar(32) | 是 | 货主 |
| inbound_date | date | 是 | 入库日期 |
| total_quantity | decimal(18,4) | 是 | 总数量 |
| goods_amount | decimal(18,2) | 是 | 入库金额 |
| status | varchar(50) | 是 | DRAFT/PENDING_AUDIT/AUDITED/CLOSED |
| stock_updated | tinyint | 是 | 是否更新库存 |
| ledger_generated | tinyint | 是 | 是否生成流水 |
| cost_calculated | tinyint | 是 | 是否计算成本 |
| receipt_generated | tinyint | 是 | 是否生成收货单 |
| auditor_id | varchar(32) | 否 | 审核人 |
| audit_time | datetime | 否 | 审核时间 |

### 4.4 采购入库明细 purchase_inbound_detail

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| detail_id | varchar(32) | 是 | 主键 |
| inbound_id | varchar(32) | 是 | 入库ID |
| order_detail_id | varchar(32) | 是 | 采购订单明细 |
| goods_id | varchar(32) | 是 | 商品 |
| unit_id | varchar(32) | 是 | 单位 |
| order_quantity | decimal(18,4) | 是 | 应入数量 |
| received_quantity | decimal(18,4) | 是 | 实收数量 |
| base_quantity | decimal(18,4) | 是 | 基本单位数量 |
| price | decimal(18,6) | 是 | 单价 |
| amount | decimal(18,2) | 是 | 金额 |
| batch_no | varchar(50) | 否 | 批次 |
| production_date | date | 否 | 生产日期 |
| expiry_date | date | 否 | 到期日期 |
| location_id | varchar(32) | 否 | 库位，V1.5使用 |
| cost_price_before | decimal(18,6) | 否 | 入库前成本 |
| cost_price_after | decimal(18,6) | 否 | 入库后成本 |

### 4.5 采购收货主表 pur_receipt

> V1.0 落地版本 —— 详见 [V16__pur_receipt.sql](../../../backend/src/main/resources/db/migration/V16__pur_receipt.sql)
> 由采购入库单审核时自动生成（一对一映射 `source_inbound_no`），审核时写 `fin_ap` 生成正式应付。
> 税额策略：逐行按明细 `tax_rate` 计算行税额，头部 `tax_amount = Σ 明细税额`。

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| receipt_id | varchar(32) | 是 | 主键 |
| receipt_no | varchar(50) | 是 | 收货单号（`PR + yyyyMMdd + 4 位流水`） |
| source_inbound_no | varchar(50) | 是 | 来源入库单号（唯一约束，保证 1:1） |
| source_order_no | varchar(50) | 否 | 来源采购订单号（透传） |
| supplier_code / supplier_name | varchar | 否 | 供应商编码 / 名称 |
| warehouse | varchar(100) | 否 | 仓库 |
| receipt_date | date | 是 | 收货日期 |
| goods_amount | decimal(18,2) | 是 | 商品金额 = Σ 明细金额（不含税） |
| tax_amount | decimal(18,2) | 是 | 税额 = Σ 明细税额（逐行按 tax_rate 计算） |
| expense_amount | decimal(18,2) | 是 | 费用分摊金额（V1.0 占位为 0） |
| final_amount | decimal(18,2) | 是 | 最终金额 = goods + tax + expense |
| ap_status | varchar(20) | 是 | 未生成 / 已生成（审核时写 fin_ap 后置为已生成） |
| pay_status | varchar(20) | 是 | 未付款 / 部分付款 / 已付款（V1.0 占位，付款单模块回写） |
| status | varchar(20) | 是 | PENDING / APPROVED / CANCELLED |
| creator_name / audit_user | varchar(100) | 否 | 创建人 / 审核人 |
| audit_time / create_time | timestamp | 否 | 审核时间 / 创建时间 |
| remark | varchar(500) | 否 | 备注 |

### 4.6 采购收货明细 pur_receipt_detail

> 按 `goods_code` 从入库单聚合（合并所有批次为一行，不含批次号 / 生产日期）。

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| detail_id | varchar(32) | 是 | 主键 |
| receipt_id | varchar(32) | 是 | 收货单 ID |
| goods_code / goods_name | varchar | 是 | 商品编码 / 名称 |
| unit_name | varchar(50) | 否 | 单位名 |
| qty | decimal(18,4) | 是 | 数量 = Σ 入库明细.received_qty by goods_code |
| price | decimal(18,4) | 是 | 单价（来源订单单价，只读） |
| amount | decimal(18,2) | 是 | 金额 = qty × price |
| tax_rate | varchar(20) | 否 | 税率（从对应采购订单明细透传） |
| tax_amount | decimal(18,2) | 是 | 行税额 = amount × tax_rate |
| remark | varchar(500) | 否 | 备注 |

**说明**：V1.0 采购流程闭环 = 采购订单(APPROVED) → 采购入库单(APPROVED，写 `inv_batch_stock` + 移动平均法) → 【自动生成】采购收货单(PENDING) → 收货单审核 → 写 `fin_ap`。反审核收货单要求 `fin_ap.paid_amount = 0`。

### 4.7 采购费用单 purchase_expense / purchase_expense_detail / purchase_expense_alloc

主表字段：`expense_id`、`expense_no`、`expense_date`、`expense_type_id`、`payee_type`、`supplier_id`、`counterparty_id`、`expense_amount`、`tax_amount`、`allocation_mode`、`generate_payable`、`status`、`audit_time`。

明细字段：`detail_id`、`expense_id`、`expense_item`、`expense_type_id`、`quantity`、`price`、`amount`、`tax_rate`、`tax_amount`。

分摊字段：`alloc_id`、`expense_id`、`inbound_id`、`inbound_detail_id`、`goods_id`、`alloc_base_amount`、`alloc_rate`、`alloc_amount`。

### 4.8 采购发票 purchase_invoice / purchase_invoice_match

主表字段：`invoice_id`、`invoice_no`、`invoice_code`、`tax_invoice_no`、`invoice_type`、`supplier_id`、`invoice_date`、`receive_date`、`amount_no_tax`、`tax_amount`、`amount_with_tax`、`cert_status`、`match_status`、`status`。

勾稽字段：`match_id`、`invoice_id`、`receipt_id`、`payable_id`、`matched_amount`、`matched_tax_amount`、`difference_amount`。

---

## 五、销售业务表

### 5.1 销售订单主表 sales_order

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| order_id | varchar(32) | 是 | 主键 |
| order_no | varchar(50) | 是 | 销售单号 |
| customer_id | varchar(32) | 是 | 客户 |
| customer_name | varchar(200) | 是 | 客户名称 |
| customer_address_id | varchar(32) | 是 | 收货地址 |
| salesman_id | varchar(32) | 否 | 业务员 |
| warehouse_id | varchar(32) | 是 | 发货仓库 |
| owner_id | varchar(32) | 是 | 货主 |
| order_date | date | 是 | 单据日期 |
| delivery_date | date | 否 | 要求送达 |
| goods_amount | decimal(18,2) | 是 | 商品金额 |
| discount_amount | decimal(18,2) | 是 | 折扣金额 |
| final_amount | decimal(18,2) | 是 | 订单金额 |
| received_amount | decimal(18,2) | 是 | 已收金额 |
| unpaid_amount | decimal(18,2) | 是 | 未收金额 |
| settlement_type | varchar(50) | 是 | 结算方式 |
| credit_check_status | varchar(50) | 是 | PASS/WARN/BLOCK |
| stock_check_status | varchar(50) | 是 | PASS/FAIL |
| price_check_status | varchar(50) | 是 | PASS/WARN/AUTHORIZED |
| status | varchar(50) | 是 | DRAFT/PENDING_AUDIT/AUDITED/PARTIAL_OUT/OUTBOUND/RECEIVED/COMPLETED/CLOSED |
| outbound_status | varchar(50) | 是 | NOT_OUT/PARTIAL_OUT/ALL_OUT |
| receive_status | varchar(50) | 是 | NOT_RECEIVED/PARTIAL_RECEIVED/ALL_RECEIVED |
| source | varchar(50) | 是 | PC/IMPORT/API/B2B预留 |
| auditor_id | varchar(32) | 否 | 审核人 |
| audit_time | datetime | 否 | 审核时间 |

### 5.2 销售订单明细 sales_order_detail

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| detail_id | varchar(32) | 是 | 主键 |
| order_id | varchar(32) | 是 | 销售订单ID |
| line_no | int | 是 | 行号 |
| goods_id | varchar(32) | 是 | 商品 |
| goods_code/name/spec | varchar | 是 | 冗余商品信息 |
| unit_id | varchar(32) | 是 | 销售单位 |
| unit_name | varchar(50) | 是 | 单位名称 |
| conversion_rate | decimal(18,6) | 是 | 换算率 |
| quantity | decimal(18,4) | 是 | 销售数量 |
| base_quantity | decimal(18,4) | 是 | 基本单位数量 |
| original_price | decimal(18,6) | 是 | 原价 |
| actual_price | decimal(18,6) | 是 | 实际价 |
| discount_amount | decimal(18,2) | 是 | 折扣金额 |
| amount | decimal(18,2) | 是 | 行金额 |
| price_source | varchar(50) | 是 | CUSTOMER/LEVEL/STANDARD/MANUAL |
| sales_type | varchar(50) | 是 | NORMAL/GIFT/PROMOTION |
| is_gift | tinyint | 是 | 是否赠品 |
| delivered_quantity | decimal(18,4) | 是 | 已出库数量 |
| received_quantity | decimal(18,4) | 是 | 已签收数量 |
| returned_quantity | decimal(18,4) | 是 | 已退货数量 |
| stock_lock_id | varchar(32) | 否 | 锁定记录 |

### 5.3 销售出库主表 sales_outbound

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| outbound_id | varchar(32) | 是 | 主键 |
| outbound_no | varchar(50) | 是 | 出库单号 |
| order_id | varchar(32) | 是 | 销售订单 |
| order_no | varchar(50) | 是 | 销售单号 |
| customer_id | varchar(32) | 是 | 客户 |
| warehouse_id | varchar(32) | 是 | 出库仓库 |
| owner_id | varchar(32) | 是 | 货主 |
| outbound_date | date | 是 | 出库日期 |
| total_quantity | decimal(18,4) | 是 | 出库数量 |
| goods_amount | decimal(18,2) | 是 | 销售金额 |
| cost_amount | decimal(18,2) | 是 | 出库成本 |
| status | varchar(50) | 是 | DRAFT/PENDING_AUDIT/AUDITED/CLOSED |
| stock_updated | tinyint | 是 | 是否扣库存 |
| ledger_generated | tinyint | 是 | 是否生成流水 |
| receipt_generated | tinyint | 是 | 是否生成销售收货单 |
| auditor_id | varchar(32) | 否 | 审核人 |
| audit_time | datetime | 否 | 审核时间 |

### 5.4 销售出库明细 sales_outbound_detail

字段：`detail_id`、`outbound_id`、`order_detail_id`、`goods_id`、`unit_id`、`outbound_quantity`、`base_quantity`、`sale_price`、`sale_amount`、`cost_price`、`cost_amount`、`batch_no`、`location_id`。

### 5.5 销售收货主表 sales_receipt

> V1.0 落地版本 —— 详见 [V17__sales_close_loop.sql](../../../backend/src/main/resources/db/migration/V17__sales_close_loop.sql)
> 由销售出库单审核时自动生成（一对一映射 `source_outbound_no`），审核时写 `fin_ar` 生成正式应收。
> 税额策略：逐行按明细 `tax_rate` 计算行税额，头部 `tax_amount = Σ 明细税额`。

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| receipt_id | varchar(32) | 是 | 主键 |
| receipt_no | varchar(50) | 是 | 收货单号（`SR + yyyyMMdd + 4 位流水`） |
| source_outbound_no | varchar(50) | 是 | 来源出库单号（唯一约束，1:1） |
| source_order_no | varchar(50) | 否 | 来源销售订单号（透传） |
| customer_code / customer_name | varchar | 否 | 客户编码 / 名称 |
| warehouse | varchar(100) | 否 | 仓库 |
| receipt_date | date | 是 | 收货日期 |
| goods_amount | decimal(18,2) | 是 | 商品金额 = Σ 明细金额（不含税） |
| tax_amount | decimal(18,2) | 是 | 税额 = Σ 明细税额（逐行按 tax_rate 计算） |
| expense_amount | decimal(18,2) | 是 | 费用分摊金额（V1.0 占位为 0） |
| final_amount | decimal(18,2) | 是 | 最终应收金额 = goods + tax + expense |
| ar_status | varchar(20) | 是 | 未生成 / 已生成 |
| receive_status | varchar(20) | 是 | 未收款 / 部分收款 / 已收款（V1.0 占位，收款单模块回写） |
| status | varchar(20) | 是 | PENDING / APPROVED / CANCELLED |
| creator_name / audit_user | varchar(100) | 否 | 创建人 / 审核人 |
| audit_time / create_time | timestamp | 否 | 审核时间 / 创建时间 |
| remark | varchar(500) | 否 | 备注 |

### 5.6 销售收货明细 sales_receipt_detail

> 按 `goods_code` 从出库单聚合（合并所有批次为一行，不含批次号）。

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| detail_id | varchar(32) | 是 | 主键 |
| receipt_id | varchar(32) | 是 | 收货单 ID |
| goods_code / goods_name | varchar | 是 | 商品编码 / 名称 |
| unit_name | varchar(50) | 否 | 单位名 |
| qty | decimal(18,4) | 是 | 数量 = Σ 出库明细.qty by goods_code |
| price | decimal(18,4) | 是 | 单价（来源订单单价，只读） |
| amount | decimal(18,2) | 是 | 金额 = qty × price |
| tax_rate | varchar(20) | 否 | 税率（从对应销售订单明细透传） |
| tax_amount | decimal(18,2) | 是 | 行税额 = amount × tax_rate |
| remark | varchar(500) | 否 | 备注 |

**说明**：V1.0 销售流程闭环 = 销售订单(APPROVED) → 销售出库单(APPROVED，扣减 `inv_batch_stock`) → 【自动生成】销售收货单(PENDING) → 收货单审核 → 写 `fin_ar`。反审核收货单要求 `fin_ar.received_amount = 0`。

---

## 六、库存表

### 6.1 库存余额表 inv_stock_balance

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| balance_id | varchar(32) | 是 | 主键 |
| owner_id | varchar(32) | 是 | 货主 |
| goods_id | varchar(32) | 是 | 商品 |
| warehouse_id | varchar(32) | 是 | 仓库 |
| base_unit_id | varchar(32) | 是 | 基本单位 |
| physical_qty | decimal(18,4) | 是 | 实物库存 |
| locked_qty | decimal(18,4) | 是 | 锁定库存 |
| frozen_qty | decimal(18,4) | 是 | 冻结库存 |
| available_qty | decimal(18,4) | 是 | 可用库存 |
| purchase_in_transit_qty | decimal(18,4) | 是 | 采购在途 |
| cost_group | varchar(50) | 是 | 成本分组 |
| cost_price | decimal(18,6) | 是 | 当前成本 |
| stock_amount | decimal(18,2) | 是 | 库存金额 |
| updated_at | datetime | 是 | 更新时间 |

唯一建议：`owner_id + goods_id + warehouse_id + cost_group`。

### 6.2 批次库存表 inv_batch_stock

字段：`batch_stock_id`、`owner_id`、`goods_id`、`warehouse_id`、`batch_no`、`production_date`、`expiry_date`、`physical_qty`、`locked_qty`、`available_qty`、`status`。

### 6.3 库存锁定表 inv_stock_lock

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| lock_id | varchar(32) | 是 | 主键 |
| source_bill_type | varchar(50) | 是 | 来源单据类型 |
| source_bill_id | varchar(32) | 是 | 来源单据ID |
| source_bill_no | varchar(50) | 是 | 来源单号 |
| owner_id | varchar(32) | 是 | 货主 |
| goods_id | varchar(32) | 是 | 商品 |
| warehouse_id | varchar(32) | 是 | 仓库 |
| batch_no | varchar(50) | 否 | 批次 |
| lock_qty | decimal(18,4) | 是 | 锁定数量 |
| lock_status | varchar(50) | 是 | LOCKED/RELEASED/DEDUCTED |
| expire_time | datetime | 否 | 过期时间 |
| released_at | datetime | 否 | 释放时间 |

### 6.4 库存流水表 inv_stock_ledger

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| ledger_id | varchar(32) | 是 | 主键 |
| ledger_no | varchar(50) | 是 | 流水号 |
| source_bill_type | varchar(50) | 是 | 来源类型 |
| source_bill_id | varchar(32) | 是 | 来源ID |
| source_bill_no | varchar(50) | 是 | 来源单号 |
| owner_id | varchar(32) | 是 | 货主 |
| warehouse_id | varchar(32) | 是 | 仓库 |
| goods_id | varchar(32) | 是 | 商品 |
| batch_no | varchar(50) | 否 | 批次 |
| direction | varchar(10) | 是 | IN/OUT |
| quantity | decimal(18,4) | 是 | 数量 |
| cost_price | decimal(18,6) | 是 | 成本单价 |
| amount | decimal(18,2) | 是 | 成本金额 |
| balance_qty_after | decimal(18,4) | 是 | 变动后结存 |
| cost_price_after | decimal(18,6) | 是 | 变动后成本 |
| operator_id | varchar(32) | 否 | 操作人 |
| occurred_at | datetime | 是 | 发生时间 |
| reverse_ledger_id | varchar(32) | 否 | 反向流水ID |

### 6.5 库存调整表 inv_stock_adjust / inv_stock_adjust_detail

主表：调整单号、仓库、调整原因、状态、审核人、审核时间。  
明细：商品、批次、调整前数量、调整后数量、差异数量、成本单价、金额。

### 6.6 其他入库表 inv_other_inbound / inv_other_inbound_detail

主表字段：`inbound_id`、`inbound_no`、`inbound_date`、`warehouse_id`、`reason_type`、`affect_cost`、`total_quantity`、`total_amount`、`status`、`audit_time`。

明细字段：`detail_id`、`inbound_id`、`goods_id`、`unit_id`、`quantity`、`price`、`amount`、`cost_price`、`cost_amount`、`batch_no`、`production_date`、`expiry_date`。

### 6.7 其他出库表 inv_other_outbound / inv_other_outbound_detail

主表字段：`outbound_id`、`outbound_no`、`outbound_date`、`warehouse_id`、`reason_type`、`department_id`、`receiver_id`、`total_quantity`、`cost_amount`、`status`、`audit_time`。

明细字段：`detail_id`、`outbound_id`、`goods_id`、`unit_id`、`quantity`、`cost_price`、`cost_amount`、`batch_no`。

### 6.8 报损单 inv_damage_order / inv_damage_order_detail

主表字段：`damage_id`、`damage_no`、`damage_date`、`warehouse_id`、`damage_type`、`apply_user_id`、`department_id`、`total_quantity`、`cost_amount`、`status`、`audit_time`。

明细字段：`detail_id`、`damage_id`、`goods_id`、`unit_id`、`batch_no`、`available_qty`、`damage_qty`、`cost_price`、`cost_amount`、`damage_reason`、`attachment_urls`。

### 6.9 成本调整表 inv_cost_adjust / inv_cost_adjust_detail

主表字段：`adjust_id`、`adjust_no`、`adjust_date`、`warehouse_id`、`cost_group`、`adjust_reason`、`adjust_mode`、`status`、`audit_time`。

明细字段：`detail_id`、`adjust_id`、`goods_id`、`batch_no`、`stock_qty`、`cost_price_before`、`stock_amount_before`、`cost_price_after`、`stock_amount_after`、`diff_amount`。

---

## 七、财务表

### 7.1 应收表 fin_receivable

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| receivable_id | varchar(32) | 是 | 主键 |
| receivable_no | varchar(50) | 是 | 应收单号 |
| source_bill_type | varchar(50) | 是 | SALES_RECEIPT/SALES_RETURN等 |
| source_bill_id | varchar(32) | 是 | 来源ID |
| source_bill_no | varchar(50) | 是 | 来源单号 |
| customer_id | varchar(32) | 是 | 客户 |
| customer_name | varchar(200) | 是 | 客户名称 |
| salesman_id | varchar(32) | 否 | 业务员 |
| bill_date | date | 是 | 业务日期 |
| receivable_amount | decimal(18,2) | 是 | 应收金额 |
| received_amount | decimal(18,2) | 是 | 已收金额 |
| unpaid_amount | decimal(18,2) | 是 | 未收金额 |
| expected_payment_date | date | 是 | 预计结款日 |
| latest_payment_date | date | 否 | 最近结款日 |
| writeoff_status | varchar(50) | 是 | NOT/PARTIAL/DONE |
| overdue_days | int | 是 | 逾期天数 |
| invoice_status | varchar(50) | 是 | NOT/PARTIAL/DONE |
| status | varchar(50) | 是 | NORMAL/CLOSED/REVERSED |

### 7.2 应付表 fin_payable

字段与应收类似，主体为供应商：`payable_id`、`payable_no`、`source_bill_type`、`supplier_id`、`payable_amount`、`paid_amount`、`unpaid_amount`、`expected_pay_date`、`writeoff_status`、`status`。

### 7.3 收款单 fin_receipt

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| receipt_id | varchar(32) | 是 | 主键 |
| receipt_no | varchar(50) | 是 | 收款单号 |
| receipt_date | date | 是 | 收款日期 |
| customer_id | varchar(32) | 否 | 客户 |
| counterparty_id | varchar(32) | 否 | 往来单位 |
| receipt_type | varchar(50) | 是 | SALES/PRE_RECEIVE/OTHER |
| payment_method | varchar(50) | 是 | CASH/WECHAT/ALIPAY/BANK |
| fund_account_id | varchar(32) | 是 | 入账账户 |
| amount | decimal(18,2) | 是 | 收款金额 |
| written_off_amount | decimal(18,2) | 是 | 已核销金额 |
| unwritten_amount | decimal(18,2) | 是 | 未核销金额 |
| status | varchar(50) | 是 | DRAFT/AUDITED/CLOSED |
| auditor_id | varchar(32) | 否 | 审核人 |
| audit_time | datetime | 否 | 审核时间 |
| attachment_urls | text | 否 | 附件 |

### 7.4 付款单 fin_payment

字段与收款单类似，主体为供应商/往来单位：`payment_id`、`payment_no`、`payment_date`、`supplier_id`、`counterparty_id`、`payment_type`、`payment_method`、`fund_account_id`、`supplier_bank_account_id`、`payee_account_name`、`payee_bank_name`、`payee_bank_account`、`amount`、`written_off_amount`、`unwritten_amount`、`status`。

其中 `supplier_bank_account_id` 用于记录本次付款选择的供应商收款账户；付款单审核后应固化收款户名、开户行、银行账号快照，避免供应商后续修改账户影响历史付款记录。

### 7.5 收款核销表 fin_receipt_writeoff

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| writeoff_id | varchar(32) | 是 | 主键 |
| writeoff_no | varchar(50) | 是 | 核销号 |
| receipt_id | varchar(32) | 是 | 收款单ID |
| receivable_id | varchar(32) | 是 | 应收ID |
| customer_id | varchar(32) | 是 | 客户ID |
| writeoff_amount | decimal(18,2) | 是 | 核销金额 |
| writeoff_date | date | 是 | 核销日期 |
| status | varchar(50) | 是 | NORMAL/REVERSED |
| reverse_time | datetime | 否 | 反核销时间 |

### 7.6 付款核销表 fin_payment_writeoff

字段与收款核销类似：`payment_id`、`payable_id`、`supplier_id`、`writeoff_amount`。

### 7.7 资金流水表 fin_fund_ledger

| 字段 | 类型 | 必填 | 说明 |
|---|---|:---:|---|
| ledger_id | varchar(32) | 是 | 主键 |
| ledger_no | varchar(50) | 是 | 流水号 |
| fund_account_id | varchar(32) | 是 | 资金账户 |
| direction | varchar(10) | 是 | IN/OUT |
| amount | decimal(18,2) | 是 | 金额 |
| balance_before | decimal(18,2) | 是 | 变动前余额 |
| balance_after | decimal(18,2) | 是 | 变动后余额 |
| source_bill_type | varchar(50) | 是 | RECEIPT/PAYMENT |
| source_bill_id | varchar(32) | 是 | 来源ID |
| source_bill_no | varchar(50) | 是 | 来源单号 |
| occurred_at | datetime | 是 | 发生时间 |

### 7.8 费用单 fin_expense / fin_expense_detail

主表字段：`expense_id`、`expense_no`、`expense_date`、`direction`、`expense_type_id`、`object_type`、`customer_id`、`supplier_id`、`counterparty_id`、`amount`、`tax_amount`、`generate_ar_ap`、`direct_pay_receive`、`fund_account_id`、`status`、`audit_time`。

明细字段：`detail_id`、`expense_id`、`expense_item`、`expense_type_id`、`quantity`、`price`、`amount`、`tax_rate`、`tax_amount`、`remark`。

### 7.9 应收结算表 fin_ar_settlement / fin_ar_settlement_detail

主表字段：`settlement_id`、`settlement_no`、`object_type`、`customer_id`、`counterparty_id`、`settlement_date`、`ar_amount`、`receipt_amount`、`discount_amount`、`balance_after`、`receipt_id`、`status`。

明细字段：`detail_id`、`settlement_id`、`receivable_id`、`source_bill_no`、`unpaid_amount`、`settle_amount`、`discount_amount`。

### 7.10 应付结算表 fin_ap_settlement / fin_ap_settlement_detail

主表字段：`settlement_id`、`settlement_no`、`object_type`、`supplier_id`、`counterparty_id`、`settlement_date`、`ap_amount`、`payment_amount`、`discount_amount`、`balance_after`、`payment_id`、`status`。

明细字段：`detail_id`、`settlement_id`、`payable_id`、`source_bill_no`、`unpaid_amount`、`settle_amount`、`discount_amount`。

---

## 八、报表与导入导出表

### 8.1 导入记录 sys_import_record

字段：导入ID、模块、文件名、总数、成功数、失败数、失败文件路径、状态、导入人、导入时间。

### 8.2 导出记录 sys_export_record

字段：导出ID、模块、文件名、文件路径、总数、状态、失败原因、创建时间、完成时间、过期时间。

---

## 九、关键索引建议

| 表 | 索引 |
|---|---|
| base_goods | goods_code唯一、base_barcode、goods_name、category_id |
| base_customer | customer_code唯一、customer_name、salesman_id、status |
| base_supplier | supplier_code唯一、supplier_name、status |
| purchase_order | order_no唯一、supplier_id、order_date、status |
| purchase_inbound | inbound_no唯一、order_id、supplier_id、audit_time |
| sales_order | order_no唯一、customer_id、order_date、status、salesman_id |
| sales_outbound | outbound_no唯一、order_id、customer_id、audit_time |
| inv_stock_balance | owner_id+goods_id+warehouse_id+cost_group唯一 |
| inv_stock_ledger | source_bill_no、goods_id、warehouse_id、occurred_at |
| fin_receivable | receivable_no唯一、customer_id、writeoff_status、expected_payment_date |
| fin_payable | payable_no唯一、supplier_id、writeoff_status、expected_pay_date |
| fin_fund_ledger | fund_account_id、occurred_at、source_bill_no |

---

## 十、后续技术设计需确认事项

1. 是否强制数据库外键。
2. 单据号生成方案。
3. 乐观锁和并发控制方案。
4. 库存扣减是否使用数据库行锁、Redis锁或库存服务统一扣减。
5. 成本重算是否异步化。
6. 应收应付是否支持红字单据独立表或同表负数。
7. 导入导出文件存储位置。
8. 审计字段是否统一由框架自动填充。
9. 多租户字段是否V1.0启用。

---

*文档结束*