# 优化记录 — 总账验证环境标题标识（区分两套并行前端）

- **分支**：`feat/finance-gl-aux-export`（工作树 `E:\work\erp-wms-tms-gl`）
- **迁移**：无（纯前端）
- **日期**：2026-09-08

## 背景

总账验证期间本机并行跑两套环境：

| 地址 | 工作树 | 后端 | 总账菜单 |
| --- | --- | --- | --- |
| `http://localhost:5173` | 主树（他人分支 WIP） | 8080 | ❌ 无 |
| `http://localhost:5174` | GL 开发树 | 8082 | ✅ 有 |

两个页面浏览器标签标题同为「商贸云 ERP V1.0」，核验时极易开错地址，表现为"菜单树看不到总账管理"。

## 改动

- `frontend/src/layout/AppShell.vue`：顶栏品牌名改为读取 `import.meta.env.VITE_APP_TITLE`（默认仍为「商贸云 ERP V1.0」），并在其设置 `document.title`；新增 `VITE_APP_BADGE` 环境徽标（橙色圆角标签）。
- `frontend/src/views/LoginPage.vue`：登录页品牌名同步读取 `VITE_APP_TITLE`、展示徽标、设置标签标题——**登录前**即可区分环境。
- `frontend/src/styles/app.css`：新增 `.env-badge` 样式（橙底橙边，与主色蓝区分，醒目但不干扰）。
- 不注入环境变量时（主干日常开发、生产构建）标题与界面完全不变，对主树零影响。

## 验证环境启动方式（5174）

```bash
cd frontend
VITE_API_TARGET=http://localhost:8082 \
VITE_APP_TITLE='商贸云 ERP · 总账版（验证 5174→8082）' \
VITE_APP_BADGE='总账验证版' \
npm run dev -- --port 5174 --host 0.0.0.0
```

启动后效果：浏览器标签标题为「商贸云 ERP · 总账版（验证 5174→8082）」，登录页与顶栏品牌名旁带橙色「总账验证版」徽标；`/api` 代理指向 8082。

## 验证

- `curl 5174/src/layout/AppShell.vue` 编译产物含注入标题「商贸云 ERP · 总账版（验证 5174→8082）」与徽标「总账验证版」；
- 经 5174 代理 `POST /api/auth/login` 成功（→8082）；
- 5174 下发的 `fallback-menus.js` 仍含「总账管理」分组；5173 旧环境进程未受影响。
