# 2026-08-06 工作日志

## 销售退货开发方案（无司机流程）

- 输出方案文档：`docs/销售退货-开发方案-无司机流程.md`
- 本轮不含司机回收环节，只做退货申请 → 退货入库 → 退货入账三阶段
- 设计完全对称于采购退货三表实现（pur_return_apply/outbound/return），方向相反
  - sales_return_apply（退货申请）— BY_BILL/BY_GOODS 双模式
  - sales_return_inbound（退货入库）— 审核时增加库存，复用 InventoryCostService.purchaseInbound()
  - sales_return（退货单）— 审核写负向 fin_ar 冲减客户应收
- 退货入库成本 = 当前移动加权平均成本，复用 purchaseInbound 入参 costPrice=当前均价，均价不变
- 退款无司机现场退现金，全部走 AR 系统抵扣或付款单退还
- BillNoGenerator 已预留编号：XTSQ/THRK/XSTH
- 数据库迁移将新增 V28__sales_return.sql（6张表）
- 方案与全链路设计文档衔接：后续加司机环节不影响三表结构

## 修复：前端路由缺失导致退货等模块无法打开

- **根因**：`router/index.js` 中有 30 个菜单项缺少对应路由
- 菜单通过 `codeToPath('salesReturn')` 将 camelCase → kebab-case (`/sales-return`)，但路由表中未注册
- 涉及的模块：采购增强(2)、销售增强(4)、库存管理(8)、财务管理(10)、系统管理(6)
- **修复**：在 `router/index.js` 中补全所有 30 个缺失路由，统一使用 `GenericBusinessList.vue` 组件
- 验证：销售退货 4 个页面均返回 HTTP 200，后端 API `/api/sales/return/page` 正常

## 销售退货申请模块 — 前端开发（全部完成 ✅）

- 新建3个组件：SalesReturnApplyDrawer.vue (1040行)、SalesReturnGoodsPickerDialog.vue (343行)、SalesOutboundPickerDialog.vue (438行)
- 修改6个集成点：module-config.js、module-api.js、router/index.js、stores/app.js、AppShell.vue、GenericBusinessList.vue
- fallback-menus.js 新增销售退货申请 + 销售退货入库菜单项
- 后端 SalesReturnController 19个端点（申请/入库/退货单 CRUD + 审核/反审核）
- V28 数据库迁移：6张表三表联动

## 修复：销售退货申请"按单选择商品"报错

- 根因：后端未重启，旧代码未加载 SalesReturnController 端点
- 解决：重启后端即可

## 优化：商品选择窗口去"客户商品"页签

- SalesReturnGoodsPickerDialog 只保留"历史销售"和"全部商品"
- 历史销售后端已按 `ORDER BY MAX(h.bill_date) DESC` 排序

## 优化：销售退货申请去批次号/生产日期/日期字段

- SalesReturnApplyDrawer：移除明细的批次号、生产日期列，移除单据日期字段
- 后端 createApply/updateApply：billDate 改用 LocalDate.now()
- 后端 insertApplyDetail：batch_no、production_date 改为 null
- 后端 generateInboundFromApply：明细行 batch_no、production_date 置 null

## 新建：销售退货入库抽屉 + 行级仓库

- V29 迁移：sales_return_inbound_detail 加 warehouse 列
- 后端变更：
  - inboundPage 去 warehouse 列
  - updateInbound 接受行级 warehouse，允许 qty>=0
  - auditInbound 按行级仓库回库（非头部仓库），qty=0 跳过
- SalesReturnInboundDrawer.vue 重写（440行）：
  - 生产日期选择 → 自动生成批次号（PD+yyyMMdd），不选库内批次
  - 生产日期列在批次号列前
  - 去删除按钮，qty=0 代表零入库（行灰化显示）
  - 行级仓库下拉（加载 warehouse/page 接口）
  - 头部去仓库字段
- module-config.js：salesReturnInbound 去"仓库" filter/column

## 销售退货流程简化改造（全部完成 ✅）

- 三模块简化 → 两模块：销售退货申请→退货入库→退货单 改为 销售退货单→退货入库单
- DB V30：sales_return_apply 新增 return_qty/inbound_qty/confirmed_time/confirmed_user；删除 sales_return/sales_return_detail 表
- 后端：API 路径 /sales/return-apply/* → /sales/return-order/*；新增 confirm/reject 端点；audit 直接写 AR
- 前端：
  - 模块键 salesReturnApply → salesReturn（module-config.js / module-api.js / router / stores / fallback-menus / AppShell / GenericBusinessList 全部统一）
  - 组件重命名：SalesReturnApplyDrawer.vue → SalesReturnDrawer.vue
  - 状态流：DRAFT → PENDING → CONFIRMED/REJECTED → APPROVED
- 验证：所有源文件无 salesReturnApply 残留
